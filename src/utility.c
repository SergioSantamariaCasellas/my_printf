/*
** EPITECH PROJECT, 2024
** miniprintf
** File description:
** simplified printf
*/

#include "../include/my.h"


/*char *int_to_str(int num)
{
    char *str = malloc(sizeof(char) * 11);
    int digit;
    char digit_char;
    //my_strcpy(str, "");

    while (num > 1) {
        digit = num%10;
        digit_char = digit + 48;
        my_strcat(str, &digit_char);
        num = num/10;
    }
    my_revstr(str);
    return str;
}*/
